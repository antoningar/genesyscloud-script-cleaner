{
  "name": "lambda-function-template",
  "version": "1.0.0",
  "description": "Lambda function template with hexagonal architecture",
  "main": "handler.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "test": "cucumber-js",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist",
    "zip": "npm run clean && npm install --omit=dev && npm run build && zip -r function-script-cleaner.zip dist/ node_modules/"
  },
  "dependencies": {
    "purecloud-platform-client-v2": "*",
    "@types/node": "^22",
    "ts-node": "*",
    "@types/aws-lambda": "*"
  },
  "devDependencies": {
    "@cucumber/cucumber": "*",
    "typescript": "^5",
    "@typescript-eslint/eslint-plugin": "*",
    "@typescript-eslint/parser": "*",
    "eslint": "*"
  },
  "keywords": [
    "lambda",
    "aws",
    "typescript",
    "hexagonal-architecture",
    "purecloud"
  ],
  "author": "",
  "license": "ISC"
}